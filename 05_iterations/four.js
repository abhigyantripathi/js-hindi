const obj={
    js:"javascript",
    rb:"ruby",
    cpp:"c++",
    swift: "swift by apple"
}
//using for in loop for objects:
for (const key in obj) {
    console.log(`${key} shortcut is for ${obj[key]}`)
    }

    // key access krne ke liye direct key likhdo, value ke liye objectName[key] likhdo

    